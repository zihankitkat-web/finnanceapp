name: finnanceapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Paket ikon default Flutter
  cupertino_icons: ^1.0.8

  # Paket animasi yang digunakan di HomeScreen
  animate_do: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Paket linter untuk menjaga kualitas kode
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
